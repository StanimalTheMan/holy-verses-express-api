import { Router } from 'express'

const router = Router()

router.get('/verse', () => {})
router.get('/verse/:id', () => {})
router.put('/verse/:id', () => {})
router.post('/verse', () => {})
router.delete('/verse/:id', () => {})